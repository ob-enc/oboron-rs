[package]
name = "cli-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
oboron-cli = { path = "../oboron-cli", features = ["ztier"] }
assert_cmd = "2.0"
predicates = "3.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
